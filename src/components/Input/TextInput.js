import React from 'react';
import { View, TextInput as Input } from 'react-native';

const TextInput = (props) => {
  return <Input style={{ width: 100, height: 100, backgroundColor: 'red' }} />;
};

export default TextInput;
